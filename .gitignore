# Secrets & local data (never commit)
.env
backend/data/

# Python
__pycache__/
*.py[cod]
.venv*/
venv*/
*.egg-info/
# stray nested PoC venv from an early experiment
backend/backend/

# Audio working dirs
backend/uploads/
backend/outputs/
backend/smoke_out/
*.wav
*.mp3
*.flac
*.m4a

# Node / Angular
node_modules/
frontend/dist/
frontend/.angular/

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/
# Self-contained installer payload (multi-GB: bundled Python, Chromium, weights…)
src-tauri/payload/
src-tauri/payload-cuda/
# Built installer output (the .exe is ~630MB; latest.json is generated per release)
/dist/
# Updater signing keys — NEVER commit
*.key
*.key.pub
.tauri/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Model cache (torch downloads weights here by default if redirected)
.cache/
