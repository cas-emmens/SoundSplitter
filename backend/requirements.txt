# NOTE: install CUDA PyTorch first, separately, from the PyTorch index:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# Then: pip install -r requirements.txt

# Web API
fastapi
uvicorn[standard]
python-multipart

# Audio capture / IO
sounddevice
soundfile
numpy

# Source separation (htdemucs_6s) — relies on torch already being installed
demucs

# Spotify metadata + OAuth
spotipy

# Spectrogram rendering (stretch)
matplotlib

# Tab transcription: pitch tracking + polyphonic note detection.
# basic-pitch pins tensorflow<2.16 (CPU) -> requires Python 3.11 and numpy<2.
librosa
scipy
basic-pitch[onnx]
numpy<2
