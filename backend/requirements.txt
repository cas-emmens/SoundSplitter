# NOTE: install CUDA PyTorch first, separately, from the PyTorch index:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# Then: pip install -r requirements.txt

# Web API
fastapi
uvicorn[standard]
python-multipart
python-dotenv  # config.py loads backend/.env via dotenv

# Audio capture / IO
sounddevice
soundfile
numpy

# Source separation (htdemucs_6s) — relies on torch already being installed
demucs

# Spotify metadata + OAuth
spotipy

# Spectrogram rendering (stretch)
matplotlib

# Audio analysis helpers (used by separation / spectrogram).
librosa
scipy

# Tab generation: webpage capture + OCR via the image-tabs library (installed editable in
# setup.ps1). OpenCV is pinned to a numpy-1.x build so it coexists with torch/demucs; Playwright
# renders the tab page headlessly; pytesseract drives Tesseract OCR.
opencv-python==4.10.0.84
pytesseract
Pillow>=10
playwright
numpy<2
