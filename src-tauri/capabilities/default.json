{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window. The UI itself is the remote Angular app served by the backend and does not call Tauri IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
